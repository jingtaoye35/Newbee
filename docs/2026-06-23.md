【角色设定】
你现在是一位资深的量化数据架构师和 Python 工程师。我需要你帮我探索并设计一套数据模块的重构方案。
【当前痛点/背景】
现有的数据模块耦合度高，数据字段定义不清晰，缺乏自动化维护机制。我希望进行彻底重构，以满足以下 6 个核心需求：
【核心需求】
1. 独立存储机制：每种数据类型（同属一类数据）独立存储为parquet文件。例如，K线数据（KData）的日频数据应保存为独立的 KDataDaily.parquet 文件。
字段示例：包含 trading_date, stock_code, open, high, low, close, amount, volume, close_pre_adj (前复权收盘价) 等。你可以和我讨论下如何加速parquet的读取
2. 更新状态追踪：需要设计一个轻量级的元数据记录机制，用于记录每个数据文件最近一次“成功更新”的日期（用于增量更新或断点续传）。
3. 严格的数据字典：必须维护一份专门的数据字典文档（如 Markdown 或 YAML），严格且清晰地记录每个数据文件中所有字段的含义、数据类型及单位。
4. AI 协同维护机制：设计一套机制或规范，确保未来 Claude Code 在修改或扩展数据模块时，能够自动且严格地同步更新上述的“数据字典”和“更新状态记录”，避免文档与代码脱节。
5. 代码重构与审阅：基于以上设计，给出具体的代码重构方案（包括目录结构、核心类/函数设计、I/O 接口等），以便我进行人工审阅。
6. 在 docs 目录下生成数据模块的设计文档（作为数据模块开发和调用的最高纲领），Claude Code也要感知
【探索要求】
请针对上述需求，输出一份结构化的重构，内容需包含：
架构设计：推荐的目录结构和文件命名规范。
技术选型：针对元数据记录和 AI 协同维护的最佳实践方案。
接口设计：数据读写、状态查询的核心 API 伪代码或接口定义。
AI 协同规范：明确 Claude Code 在修改代码时必须遵循的 SOP（标准作业程序）。
潜在风险与替代方案：当前设计的优缺点及备选思路。
请先不要直接写业务代码，重点输出架构设计方案和AI 协同规范，等我审阅确认后再进行代码层面的重构。